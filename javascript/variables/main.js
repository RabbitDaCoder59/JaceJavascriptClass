// alert("connected!!")

        //variables
        //-var
        //-let
        //-const
        //Initialization: -> Declaring and assigning a value to a variable
        var name = "John Smith"
        console.log(name)
        //overiding a variable
        name = "Jane Doe"
        console.log(name);

        //Declartion: -> Creating a variable without any value
        var course
        course = "Javascript"
        console.log(course);
        //scope

        //let
        {
            var phone = "iphone"
            console.log(phone);
        }

       // console.log(phone);
        //const
        const country ="nigeria"
        console.log(country);
        //country = "usa"

        //Rules for creating variables
        // - The variable name must not start with a digit
        // - There should not be a whitespace between your variable name
        // - You can't have the same variable name multiple times
        // - Your variable name can start with either and underscore (_)
        //   or a letter () and it cannot contain symbols.  
        // CASING
        //eg camel,: productPriceName
        // pascal,: ProductPriceNmae
        // snake,: product_price_name for const PRODUCT_PRICE_NAME
        // kebeb case;use in css: card-text 